# /home/saintno/.config/tmuxinator/frontend.yml

name: halox
root: ~/Sources/Halocom/board

windows:
  - editor:
      layout: fa56,190x53,0,0{133x53,0,0,0,56x53,134,0[56x26,134,0,1,56x26,134,27,2]}
      # Synchronize all panes of this window, can be enabled before or after the pane commands run.
      # 'before' represents legacy functionality and will be deprecated in a future release, in favour of 'after'
      # synchronize: after
      panes:
        - nvim
        - git log
        -
